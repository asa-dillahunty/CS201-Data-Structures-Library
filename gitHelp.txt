git add
	Adds files to the repo

git rm
	removes files from the repo

git commit -m "Comment"
	Makes a commit with commit notes

git push origin master
	updates the git repo

git pull
	Updates the local repo
